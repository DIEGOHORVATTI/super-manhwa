node_modules/
dist/
*.log
.DS_Store
.env
.env.*
!.env.example
!**/.env.example

# build artifacts (no leading slash → match at any depth, incl. apps/*/)
.next
.output
.vercel
.wrangler

data
# Local deploy helper(s) with embedded server credentials — never commit
scripts/*.local.sh
