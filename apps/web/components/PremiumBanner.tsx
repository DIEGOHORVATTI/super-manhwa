/**
 * Subscription CTA | retired now that translation / language-learning is free for
 * everyone. Kept as a no-op so existing call sites don't need to change; restore
 * the banner here if a paid tier ever returns.
 */
export function PremiumBanner(_props: { compact?: boolean }) {
  return null;
}
