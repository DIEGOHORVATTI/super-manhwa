/**
 * Animated "side rays" backdrop | a lightweight CSS take on reactbits' Side Rays
 * (no WebGL/ogl dependency). Two fans of accent-coloured light beams sweep in
 * from the left and right edges and shimmer slowly. Purely decorative.
 */
export function SideRays() {
  return <div className="side-rays" aria-hidden="true" />;
}
